import styled from 'styled-components';

export const CarsDetailTemplateStyled = styled.div`
  .cars-detail-page {
    > p {
      font-size: 12px;
      font-weight: 400;

      > span {
        font-size: 14px;
        font-weight: 500;
      }
    }
  }
`;
